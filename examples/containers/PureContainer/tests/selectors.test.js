// import { fromJS } from 'immutable';
// import { makeSelectPureContainerDomain } from '../selectors';

// const selector = makeSelectPureContainerDomain();

describe('makeSelectPureContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
