// import { fromJS } from 'immutable';
// import { makeSelectContainerDomain } from '../selectors';

// const selector = makeSelectContainerDomain();

describe('makeSelectContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
