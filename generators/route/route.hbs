$1
    {
      path: '{{path}}',
      name: '{{component}}',
      getComponent(location, cb) {
        System.import('../{{{directory (properCase component)}}}')
          .then(loadModule(cb))
          .catch(errorLoading);
      }
    },
